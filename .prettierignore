node_modules
dist
package-lock.json
sunflower-land
# Captured farm fixture for the local-only dev mode (VITE_OFFLINE_FARM).
# A verbatim API payload — keep it byte-identical to what upstream returns.
src/api/offlineFarm.snapshot.json
